[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "python scripts/ensure_model.py",
]

[start]
cmd = "python scripts/ensure_model.py && gunicorn desercion_escolar.wsgi --bind 0.0.0.0:$PORT"
